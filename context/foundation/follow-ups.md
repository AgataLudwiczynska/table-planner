# Deferred Follow-ups

> Central register of review findings intentionally **deferred** rather than fixed when they
> surfaced. One row per item. The full rationale stays in the source `follow-ups/*.md` of the
> change where the finding was born — this file only **indexes and tracks status** (like
> `MEMORY.md` indexes memories).
>
> Consult this file when planning or implementing a slice (see `lessons.md` →
> "Reconcile deferred review follow-ups at plan/implement time"). Pull in any item whose
> **Target** is the slice at hand; flip it to DONE (with the commit/slice) once it lands.

## Status legend

- **OPEN** — not yet addressed.
- **PLANNED** — addressed in a written plan, not yet implemented.
- **DONE** — landed; append the commit/slice, keep briefly, then prune.

## Open / in-flight

| ID  | Target                                        | Summary                                                                                                                                                                                                                                                                                                                                                                                                                 | Status                                                                | Source                                                                                                                                                     |
| --- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F2  | future / observability                        | When a service collapses an unknown Supabase/DB error to a generic 500, log the underlying `PostgrestError` server-side (e.g. `console.error`) so 500s stay debuggable while users still get only the generic message.                                                                                                                                                                                                  | OPEN                                                                  | S-01 error-handling design discussion (`context/changes/wedding-shell-with-tables/`)                                                                       |
| F3  | future / UI polish                            | Restyle the sign-in/sign-up screens and the anonymous landing page from the dark cosmic palette to the light wedding theme (`bg-wedding`) introduced in S-01, so the whole app reads as one consistent wedding-appropriate look.                                                                                                                                                                                        | OPEN                                                                  | S-01 impl review (`context/changes/wedding-shell-with-tables/reviews/impl-review.md`)                                                                      |
| F4  | future / DX                                   | Evaluate a form-handling library (e.g. React Hook Form + zod resolver) to replace the hand-rolled per-field `useState`/validation now repeated across the rename, add-table, guest add/edit, and conflict-picker forms; would cut boilerplate and let the client reuse the endpoints' `zod` schemas.                                                                                                                    | OPEN                                                                  | `context/changes/guest-and-conflict-management/follow-ups/form-library.md`                                                                                 |
| F8  | future / UI polish (S-03 ring)                | Ring seat labels overlap on large tables: name chips are legible up to ~15 seats, but the product allows up to 30 (S-01 `zod` 1–30) and the fixed-radius ring then packs chips too tightly to read. Fix directions: scale radius with seat count, and/or switch to initials-avatars + tooltip above a threshold. Assignment + validation behaviour is unaffected — visual density only.                                 | OPEN                                                                  | `context/changes/assignment-with-realtime-conflict-validation/follow-ups/ring-legibility-large-tables.md`                                                  |
| F11 | future / DB constraint or service test | `guest_conflicts.wedding_id` cross-scope smell — the column is denormalized and guarded only by the service, so a conflict could reference a `wedding_id` that doesn't match its guests' wedding. Not an RLS/payload gap (out of scope for this phase). Target: a future DB constraint/trigger tying `wedding_id` to the guests' wedding, or a service test. | OPEN | `context/changes/testing-api-rls-integration/plan.md` (What We're NOT Doing; Phase 2 §6) |
| F12 | future / service unit-test slice | Membership-check regression guard for `seats→tables` / `guests` — the integration IDOR cell proves refusal-of-outcome only (under B's RLS-scoped client A's rows are invisible regardless of the service filter), so a real-JWT test can't isolate the service membership filter itself. Target: a future service unit-test slice. | OPEN | `context/changes/testing-api-rls-integration/reviews/plan-review.md` (finding F7); plan.md (Phase 2 §6; Phase 3 §3) |
| F10 | future / docs                                 | Rewrite `README.md` for TablePlanner. It is still the generic upstream starter README ("10x Astro Starter", links to the template repo) and never describes this product — a new reader learns nothing about the app from it (all product context lives only in `context/foundation/prd.md`). Replace with a project-specific intro: the problem (wedding seating + adjacency conflicts), what the adjacency validator does, and local setup with Supabase. Keep the useful setup/stack sections that still apply.                                    | OPEN                                                                  | MVP readiness review (2026-08-31); README criterion nuance vs. strong PRD                                                                                   |

## Done

| ID  | Target                       | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Status                                                              | Source                                                                                 |
| --- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| F6  | test-runner · S-03 guardrail | Stand up a test runner (Vitest) and cover the adjacency validator with the "validation never stays silent" guardrail. Pure `validateTable`/`validateAllTables` unit suite: adjacent flags / non-adjacent doesn't / first↔last wrap-around / n===2 modulo-collapse counted once / n===1 empty / partial occupancy / cross-table independence.                                                                                                                                                                                                                                 | DONE (`context/changes/testing-bootstrap-adjacency-core/`, 068f2f1) | S-03 planning (`context/changes/assignment-with-realtime-conflict-validation/plan.md`) |
| F7  | prod / release (S-02)        | S-02 (`guests`+`guest_conflicts`) is merged + deployed, but its migration was never pushed to the prod Supabase project — Cloudflare Workers Builds deploys code only, not migrations, so prod schema is behind the deployed code. Fix: in a low-traffic window, `npx supabase migration list` then `npx supabase db push` (linked prod); verify per the RLS runbook. Pre-launch (no real users) so harmless now, but must land before prod gets traffic. S-03's migration is not merged yet — it rides its own merge via `docs/reference/deploy-runbook.md`, not this debt. | DONE (S-02 migration pushed to prod Supabase, 2026-08-24)           | S-03 implementation (`context/changes/assignment-with-realtime-conflict-validation/`)  |
| F9  | testing-api-rls-integration (rollout Phase 2) | Assert the `guest_conflicts` `check (guest_a_id < guest_b_id)` constraint at the integration layer: a non-canonical `(B,A)` insert must be rejected (SQLSTATE 23514). | DONE (`context/changes/testing-api-rls-integration/` Phase 2 — `test/integration/db/db-check-conflicts.test.ts`) | `context/foundation/test-plan.md` §6.5 |
| F1  | S-01 (API) · S-04 (DB)       | Bound `seat_count` upper limit — RPC `create_table_with_seats` has no ceiling; add one in the API `zod` schema and a defense-in-depth guard inside the resize RPC. | DONE (S-01 API: `zod` 1–30, 198379e · S-04 DB guard: `update_table` raises `22023` for 0/31, 673c5d4) | `context/archive/2026-08-09-wedding-scope-schema-and-rls/follow-ups/review-fixes.md` |
| F5  | S-04 (table-edit-with-guest-auto-unassign) | Extract the "Stoły" tab out of `WeddingWorkspace` into its own `TablesTab.tsx` (mirroring `GuestsTab`/`ConflictsTab`) and move the table-form state into it; pure refactor for encapsulation, no behaviour change. | DONE (S-04 `TablesTab.tsx` extraction, 74174bb) | `context/changes/guest-and-conflict-management/follow-ups/extract-tables-tab.md` |
